name: proyecto_albion_tfg
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.1.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  firebase_core: ^3.10.0
  cloud_firestore: ^5.4.0
  http: ^1.2.0
  flutter_secure_storage: ^9.0.0
  provider: ^6.1.5+1
  shared_preferences: ^2.5.3
  firebase_auth: ^5.4.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - lib/asset/images/
